<section class="mt-section">
  <h2 class="text-display-sm"><%= t("problems.show.submit_title") %></h2>

  <% if contest.open? %>
    <p class="mt-xs max-w-prose text-body-md text-body"><%= t("problems.show.submit_hint") %></p>

    <%= form_with model: [ problem, Submission.new ], class: "mt-base" do |form| %>
      <%= form.label :code, t("problems.show.code"), class: "sr-only" %>
      <div class="code-editor" data-controller="code-editor">
        <pre data-code-editor-target="output" aria-hidden="true"></pre>
        <%= form.text_area :code, required: true, spellcheck: false,
              placeholder: t("problems.show.code_placeholder"),
              data: { code_editor_target: "input", action: "input->code-editor#highlight" } %>
      </div>
      <%= form.submit t("problems.show.submit"),
            class: "mt-base h-[40px] cursor-pointer rounded-md bg-primary px-md text-button text-on-primary" %>
    <% end %>
  <% else %>
    <p class="mt-xs max-w-prose text-body-md text-body"><%= t("problems.show.closed") %></p>
  <% end %>
</section>
